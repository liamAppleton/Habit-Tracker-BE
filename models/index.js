const {
  fetchUsers,
  fetchUserByUsername,
  addUser,
  updateUserByUsername,
} = require('./users.models');

module.exports = {
  fetchUsers,
  fetchUserByUsername,
  addUser,
  updateUserByUsername,
};
