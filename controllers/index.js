const {
  getUsers,
  getUserByUsername,
  postUser,
  patchUserByUsername,
} = require('./users.controllers');

module.exports = {
  getUsers,
  getUserByUsername,
  postUser,
  patchUserByUsername,
};
